<h1>Current Status of CTA Trains</h1>

<table>
  <th>Train Line</th>
  <th>Status</th>

  <% @trains.each do |train| %>
    <tr>
      <td> <%= train.name %></td>
      <td class="<%= train.label %>"><%= train.status %></td>
    </tr>
  <% end %>


</table>